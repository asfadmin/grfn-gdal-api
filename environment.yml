name: grfn-gdal-api
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.11
  - gdal=3.8.3 # Copied from requirements-reformat.txt, required for mypy workflow
  - pip
  - pip:
    - -r requirements-all.txt
